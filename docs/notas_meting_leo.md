order api needs to be redone, it will not work as expected due to the way sql databases work.

slug is a unique identifier that is more friendly named than uuid

uuid4 is default uuid field in django


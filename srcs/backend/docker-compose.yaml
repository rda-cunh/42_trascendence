services:
  backend:
    container_name: backend
    build: ./app
    environment:
      DATA_SERVICE_URL: "http://data-service:5000/data"
    depends_on:
      - data-service
    restart: always
    networks:
      - shared-network
